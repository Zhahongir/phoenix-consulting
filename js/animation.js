// Section Main
(function () {
    var titleMainOne = document.querySelector('.about__wrap__photo');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('about-photo');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();

// Section Main
(function () {
    var titleMainOne = document.querySelector('.about__wrap__content__title');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('about-title');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// Section Main
(function () {
    var titleMainOne = document.querySelector('.about__wrap__content__text');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('about-text');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();



// Section Main
(function () {
    var titleMainOne = document.querySelector('.about-text-anim');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('about-text-2');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();



// Section Main
(function () {
    var titleMainOne = document.querySelector('.advantages__title');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('advantages-title');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();



// Section Main
(function () {
    var titleMainOne = document.querySelector('.adv-item-1');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('adv-1-anim');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();

// Section Main
(function () {
    var titleMainOne = document.querySelector('.adv-item-2');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('adv-2-anim');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();

// Section Main
(function () {
    var titleMainOne = document.querySelector('.adv-item-3');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('adv-3-anim');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// Section Main
(function () {
    var titleMainOne = document.querySelector('.adv-item-4');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('adv-4-anim');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();

// Section Main
(function () {
    var titleMainOne = document.querySelector('.adv-item-5');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('adv-5-anim');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();

// Section Main
(function () {
    var titleMainOne = document.querySelector('.adv-item-6');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('adv-6-anim');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();

// Section Main
(function () {
    var titleMainOne = document.querySelector('.adv-item-7');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('adv-7-anim');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();

// Section Main
(function () {
    var titleMainOne = document.querySelector('.adv-item-8');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('adv-8-anim');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();

// Section Main
(function () {
    var titleMainOne = document.querySelector('.service__title');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('service-title');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();

// Section Main
(function () {
    var titleMainOne = document.querySelector('.service__wrap__item__header__number');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('num-1');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();

// Section Main
(function () {
    var titleMainOne = document.querySelector('.step-1');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('step-1-anim');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();

// Section Main
(function () {
    var titleMainOne = document.querySelector('.step-2');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('step-2-anim');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();

// Section Main
(function () {
    var titleMainOne = document.querySelector('.step-3');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('step-3-anim');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// Section Main
(function () {
    var titleMainOne = document.querySelector('.service__done__icon');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('check-icon');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();

// Section Main
(function () {
    var titleMainOne = document.querySelector('.service__done__text');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('check-text');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();

// Section Main
(function () {
    var titleMainOne = document.querySelector('.service__price__text');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('price-animation');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();

// Section Main
(function () {
    var titleMainOne = document.querySelector('.service__price__subtitle');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('price-text-animation');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();


// Section Main
(function () {
    var titleMainOne = document.querySelector('.service__price__btn');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('price-btn-animation');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();

// Section Main
(function () {
    var titleMainOne = document.querySelector('.include__title');
  
    var observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (typeof getCurrentAnimationPreference === 'function' && !getCurrentAnimationPreference()) {
          return;
        }
  
        if (entry.isIntersecting) {
          entry.target.classList.add('include-title-animation');
        }
      });
    });
  
    observer.observe(titleMainOne);
  })
();